% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boxplot_LR.R
\name{boxplot_LR}
\alias{boxplot_LR}
\title{Boxplot for Likelihood Ratios by Category}
\usage{
boxplot_LR(
  base,
  var_llr,
  var_categoria,
  title = "Distribución de LRs por Categoría",
  show_points = TRUE,
  show_median = TRUE,
  grid_fondo = TRUE,
  y_label = expression(log[10](LR)),
  x_label = "Categoría",
  point_alpha = 0.5,
  point_size = 1.5,
  notch = FALSE
)
}
\arguments{
\item{base}{A data frame containing the data}

\item{var_llr}{Character string with the name of the column containing likelihood ratios}

\item{var_categoria}{Character string with the name of the column containing categories
("Misma persona" and "Diferente persona")}

\item{title}{Character string for the plot title. Default: "Distribución de LRs por Categoría"}

\item{show_points}{Logical indicating whether to show individual points. Default: TRUE}

\item{show_median}{Logical indicating whether to show median labels. Default: TRUE}

\item{grid_fondo}{Logical indicating whether to show background grid. Default: TRUE}

\item{y_label}{Character string for y-axis label. Default: expression(log\href{LR}{10})}

\item{x_label}{Character string for x-axis label. Default: "Categoría"}

\item{point_alpha}{Numeric value for point transparency (0-1). Default: 0.5}

\item{point_size}{Numeric value for point size. Default: 1.5}

\item{notch}{Logical indicating whether to show notches on boxplots. Default: FALSE}
}
\value{
A ggplot2 object
}
\description{
Creates a boxplot visualization for likelihood ratios comparing same-source
and different-source categories. The plot displays the distribution of
log10-transformed likelihood ratios for both categories, with customizable
aesthetics matching the Tippett plot style.
}
\details{
The function creates a boxplot with:
\itemize{
\item Log10-transformed likelihood ratios on the y-axis
\item Two categories: "Misma persona" (same-source) and "Diferente persona" (different-source)
\item Points showing individual observations (jittered)
\item Median values displayed with labels
\item Consistent color scheme with Tippett plot
}
}
\examples{
\donttest{
# Create example data
set.seed(123)
base <- data.frame(
  LR = c(rlnorm(50, meanlog = 2, sdlog = 0.5),
         rlnorm(50, meanlog = -1, sdlog = 0.3)),
  categoria = c(rep("Misma persona", 50), rep("Diferente persona", 50))
)

# Create boxplot
boxplot_LR(base, var_llr = "LR", var_categoria = "categoria")
}

}
